spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: matthew
    password: LylitAletAlbay.14
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
  port: 8080